# Bundled FFmpeg

Place the Windows builds at:

```text
vendor/ffmpeg/bin/ffmpeg.exe
vendor/ffmpeg/bin/ffprobe.exe
```

The native build includes these files beside the packaged server. Record the chosen distribution, version, and license notices before shipping an installer.
